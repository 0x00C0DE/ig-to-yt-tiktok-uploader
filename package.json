{
  "name": "social-reel-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Synchronize Instagram Reels to independently configured YouTube and TikTok accounts.",
  "repository": "https://github.com/0x00C0DE/ig-to-yt-tiktok-uploader",
  "type": "module",
  "scripts": {
    "start": "node src/cli.js",
    "setup": "node src/cli.js setup",
    "run": "node src/cli.js transfer",
    "install:tiktok-uploader": "node scripts/install-tiktok-auto-uploader.js",
    "check": "node scripts/check-syntax.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "dependencies": {
    "playwright": "^1.54.2"
  },
  "engines": {
    "node": ">=20"
  }
}
